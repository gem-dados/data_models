config {
    type: "view",
    schema: "staging",
    description: "Normaliza raw.time_spent. Padroniza strings e remove nulos críticos.",
    assertions: {
        nonNull: ["useremail"],
        uniqueKey: ["useremail"]
    }
}

SELECT
  -- Padronização de Strings
  INITCAP(TRIM(CAST(firstname AS STRING))) AS firstname,
  INITCAP(TRIM(CAST(lastname AS STRING))) AS lastname,
  LOWER(TRIM(CAST(email AS STRING))) AS useremail,
  CAST(alltypes AS FLOAT64) AS alltypes,
  CAST(courses_classic AS FLOAT64) AS courses_classic,
  CAST(courses_ai_native AS FLOAT64) AS courses_ai_native,
  CAST(courses AS FLOAT64) AS courses,
  CAST(assessments AS FLOAT64) AS assessments,
  CAST(projects AS FLOAT64) AS projects,
  CAST(practices AS FLOAT64) AS practices,
  CURRENT_TIMESTAMP() AS loaded_at
FROM
  ${ref("raw_time_spent")}
  -- Remoção de valores nulos críticos
WHERE
  email IS NOT NULL
  AND TRIM(email) <> ""
